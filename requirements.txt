# Scraping
requests>=2.31.0
beautifulsoup4>=4.12.0
pytrends>=4.9.0

# Deduplication (TF-IDF title similarity)
scikit-learn>=1.3.0

# Timezone database for Asia/Dhaka (bundled on most Linux distros, needed on Windows)
tzdata>=2024.1

# Optional: local LLM dedup check, used only when an Ollama daemon is reachable
# ollama>=0.1.0
